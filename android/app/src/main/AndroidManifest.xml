<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Required in the main manifest so networking (Firebase, Firestore,
         Analytics, RevenueCat) works in RELEASE builds — Flutter only adds
         INTERNET to the debug/profile manifests. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- The Facebook SDK auto-merges the advertising-id permission. While Meta
         is dormant (and for Google Play Families compliance) we strip it by
         default. WHEN YOU GO LIVE with Meta Ads on Android, delete this line so
         the SDK can collect the GAID for attribution (unless you remain in the
         Families program, in which case keep it removed). -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove"/>
    <!-- Scanner: capture math problems with the back camera. The features are
         optional so devices without a camera can still install and use the
         gallery / manual-entry paths. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <application
        android:label="matheasy"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Lets Flutter (GoRouter) handle incoming deep links. -->
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="true" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Custom scheme deep links: matheasy://scan, matheasy://practice, … -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="matheasy"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ===== Meta (Facebook) SDK — App Events / Ads attribution =====
             App ID + Client Token are read from res/values/strings.xml (replace
             the placeholders there). The SDK auto-initializes at startup (do NOT
             set com.facebook.sdk.AutoInitEnabled=false — the plugin builds an
             AppEventsLogger at launch and needs the SDK initialized), but we
             keep it privacy-safe: automatic event logging and advertiser-id
             (GAID) collection stay OFF until the app explicitly enables them
             post-consent (MetaConfig + TrackingConsentController). The AD_ID
             permission the SDK would merge in is stripped by default above;
             re-enable it when going live with Meta (see that comment). -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />
        <meta-data
            android:name="com.facebook.sdk.AutoLogAppEventsEnabled"
            android:value="false" />
        <meta-data
            android:name="com.facebook.sdk.AdvertiserIDCollectionEnabled"
            android:value="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
